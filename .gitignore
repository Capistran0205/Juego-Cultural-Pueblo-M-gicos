# Godot 4+ specific ignores
.godot/
/android/

# Claves de firma / keystores (NO versionar; guardar fuera del repo)
Claves y APIS/
*.keystore
*.jks

# Los mapas (.png/.webp) SÍ se versionan, pero vía Git LFS (ver .gitattributes).
# El bundle redundante Mapas.zip (~155 MB) NO se versiona: rebasa el límite de GitHub.
Assets/Mapas/*.zip